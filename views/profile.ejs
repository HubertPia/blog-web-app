<!DOCTYPE html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />
    <link
      href="https://cdn.quilljs.com/1.3.6/quill.snow.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/main.css" />
    <title>Mój Profil</title>
  </head>
  <body data-user-id="<%= userId %>">
    <div class="user-menu-container">
      <button id="userMenuBtn" class="user-menu-btn">
        <i class="fas fa-bars"></i>
      </button>
      <div id="userMenu" class="user-menu">
        <a href="/index">Strona główna</a>
        <a href="/change-password">Zmień hasło</a>
        <a href="/logout">Wyloguj się</a>
      </div>
    </div>

    <div class="layout-container">
      <aside class="sidebar left-sidebar">
        <div class="sidebar-card user-mini-profile">
          <div class="avatar-placeholder">
            <%= name.charAt(0).toUpperCase() %>
          </div>
          <h3><%= name %></h3>
          <p style="color: #666; font-size: 0.9rem">Twój profil</p>
        </div>

        <div class="sidebar-card">
          <nav class="sidebar-nav">
            <a href="/index"> <i class="fas fa-home"></i> Strona Główna </a>
            <a
              href="/profile"
              style="color: #3b82f6; background-color: #f0f2f5"
            >
              <i class="fas fa-user-circle"></i> Mój Profil
            </a>
            <a href="/change-password">
              <i class="fas fa-cog"></i> Zmień Hasło
            </a>
            <hr style="border: 0; border-top: 1px solid #eee; margin: 10px 0" />
            <a href="/logout" style="color: #e74c3c">
              <i class="fas fa-sign-out-alt"></i> Wyloguj się
            </a>
          </nav>
        </div>
      </aside>

      <main>
        <div id="flash-message-container"></div>

        <div class="profile-stats">
          <h2>Twoje statystyki</h2>
          <div class="stats-grid">
            <div>
              <strong>Na portalu od:</strong>
              <span><%= stats.registration_date %></span>
            </div>
            <div>
              <strong>Liczba postów:</strong>
              <span><%= stats.posts_count %></span>
            </div>
            <div>
              <strong>Otrzymane polubienia:</strong>
              <span><%= stats.likes_received_count %></span>
            </div>
          </div>
        </div>

        <h3 style="margin-bottom: 20px; color: #666; padding-left: 5px">
          Twoje wpisy
        </h3>

        <div id="postsContainer">
          <% if (posts.length === 0) { %>
          <div class="post" style="text-align: center; color: #666">
            <p>Nie dodałeś jeszcze żadnych postów.</p>
          </div>
          <% } %> <% posts.forEach(post => { %>
          <div class="post" data-post-id="<%= post.id %>">
            <div class="post-meta">
              <span><%= post.creation_date %></span>
            </div>
            <h3><%= post.title %></h3>

            <div class="post-content-html"><%- post.content %></div>

            <% if (post.image_url) { %>
            <img
              src="<%= post.image_url %>"
              alt="Obrazek posta"
              class="post-image"
            />
            <% } %>

            <div class="post-actions">
              <div>
                <button
                  class="editPost"
                  data-id="<%= post.id %>"
                  data-title="<%= post.title %>"
                  data-content="<%= post.content %>"
                >
                  Edytuj
                </button>
                <button class="deletePost" data-id="<%= post.id %>">
                  Usuń
                </button>
              </div>

              <div style="display: flex; gap: 15px; align-items: center">
                <button
                  class="comment-btn toggle-comments"
                  data-id="<%= post.id %>"
                >
                  <i class="far fa-comment-alt"></i>
                  Komentarze (<span class="comments-count-label"
                    ><%= post.comments_count || 0 %></span
                  >)
                </button>
                <div class="like-container">
                  <button
                    class="like-button <%= post.is_liked ? 'liked' : '' %>"
                    data-id="<%= post.id %>"
                  >
                    <i class="fas fa-heart"></i>
                  </button>
                  <span class="likes-count"><%= post.likes_count %></span>
                </div>
              </div>
            </div>

            <div class="comments-section" id="comments-<%= post.id %>">
              <form class="add-comment-form" data-id="<%= post.id %>">
                <input
                  type="text"
                  name="comment"
                  placeholder="Napisz komentarz..."
                  required
                  autocomplete="off"
                />
                <button type="submit">Wyślij</button>
              </form>
              <div class="comments-list" id="comments-list-<%= post.id %>">
                <div
                  style="
                    text-align: center;
                    color: #999;
                    font-size: 0.9rem;
                    padding: 10px;
                  "
                >
                  Ładowanie...
                </div>
              </div>
            </div>
          </div>
          <% }) %>
        </div>
      </main>

      <aside class="sidebar right-sidebar">
        <div class="sidebar-card">
          <h3>Warto wiedzieć</h3>
          <div class="news-item">
            <strong>Edycja postów</strong>
            <span
              >Możesz edytować tytuł i treść swoich wpisów klikając przycisk
              "Edytuj".</span
            >
          </div>
          <div class="news-item">
            <strong>Usuwanie</strong>
            <span>Pamiętaj, że usunięcie posta jest nieodwracalne.</span>
          </div>
        </div>
      </aside>
    </div>

    <button id="openCreateModal">+</button>

    <div id="modalOverlayCreate" class="modal-overlay"></div>
    <div id="createPostModal" class="modal">
      <div class="modal-content">
        <span class="close" id="closeCreateModal">&times;</span>
        <h2>Dodaj nowy post</h2>
        <form class="modal-form">
          <input type="text" id="createPostTitle" placeholder="Tytuł" />

          <div class="editor-wrapper">
            <div id="createPostEditor"></div>
          </div>

          <div class="file-upload-wrapper">
            <span id="fileName"></span>
            <label for="createPostImage" class="file-btn"
              >Wybierz zdjęcie</label
            >
            <input type="file" id="createPostImage" accept="image/*" />
          </div>
          <button type="button" id="saveCreatePost">Zapisz</button>
        </form>
      </div>
    </div>

    <div id="modalOverlayEdit" class="modal-overlay"></div>
    <div id="editPostModal" class="modal">
      <div class="modal-content">
        <span class="close" id="closeEditModal">&times;</span>
        <h2>Edytuj post</h2>
        <form class="modal-form">
          <input type="hidden" id="editPostId" />
          <input type="text" id="editPostTitle" placeholder="Tytuł" />

          <div class="editor-wrapper">
            <div id="editPostEditor"></div>
          </div>

          <button type="button" id="saveEditPost">Zapisz zmiany</button>
        </form>
      </div>
    </div>

    <div id="imageModal" class="modal-overlay">
      <span class="close" id="closeImageModal">&times;</span>
      <img class="modal-image-content" id="enlargedImage" />
    </div>

    <div id="modalOverlayConfirm" class="modal-overlay"></div>
    <div id="confirmDeleteModal" class="modal">
      <div class="modal-content">
        <h2>Potwierdź usunięcie</h2>
        <p>
          Czy na pewno chcesz trwale usunąć ten post? Tej operacji nie można
          cofnąć.
        </p>
        <div class="modal-buttons">
          <button id="cancelDeleteBtn" class="btn-secondary">Anuluj</button>
          <button id="confirmDeleteBtn" class="btn-danger">Tak, usuń</button>
        </div>
      </div>
    </div>

    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <script src="/scripts/script.js"></script>
  </body>
</html>
